let intialState = {
    
}

function reducer(state = intialState, action){
    const { type, payload, meta } = action;

    switch(type){
        default:
            return state;
    }
}

export default reducer;